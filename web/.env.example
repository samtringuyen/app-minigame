# Leave empty to use the Vite dev proxy (http://localhost:3000).
# Set this when the API is not on the same origin as the built client.
VITE_API_URL=

# Used only by `vite` to choose the proxy target.
VITE_API_PROXY=http://localhost:3000
